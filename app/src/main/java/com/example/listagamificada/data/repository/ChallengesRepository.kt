package com.example.listagamificada.data.repository

/**
 * This interface is intentionally left blank.
 * The challenge logic is handled within the ChallengesViewModel and other repositories.
 * This file is kept to prevent compilation errors from old references.
 */
interface ChallengesRepository {
    // No methods are defined here as the logic is managed elsewhere.
}
